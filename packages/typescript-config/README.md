# @config-bound/typescript-config

Shared TypeScript configuration for ConfigBound projects.

## Extend the base config

Extend the base config:

```json
{
  "extends": "@config-bound/typescript-config/base.tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  }
}
```
